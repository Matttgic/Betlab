PORT=8787
# Optional. If set, POST /api/snapshots and /api/signals require: Authorization: Bearer <token>
INGEST_TOKEN=change-me
# Local SQLite path. On ephemeral serverless filesystems use a persistent database adapter instead.
DATABASE_PATH=./data/betlab.db
